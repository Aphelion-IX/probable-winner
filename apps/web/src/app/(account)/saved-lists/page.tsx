import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function SavedListsPage() {
  return (
    <PlaceholderPage
      title="Saved lists"
      description="Saved lists for later purchase are backlog Step 18."
    />
  );
}
