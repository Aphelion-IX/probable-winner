import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function SetsPage() {
  return (
    <PlaceholderPage
      title="Sets"
      description="Set browsing needs the catalogue importer (backlog Step 5) before it can list anything real."
    />
  );
}
