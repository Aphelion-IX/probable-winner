import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function CartPage() {
  return (
    <PlaceholderPage
      title="Cart"
      description="Cart and stock reservations are backlog Step 12 — nothing to add yet without a catalogue."
    />
  );
}
