import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function RecentlyAddedPage() {
  return (
    <PlaceholderPage
      title="Recently added"
      description="A live feed of newly received inventory — backlog Step 20."
    />
  );
}
