import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function StaffSettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Staff permissions and store settings already have real schema (backlog Step 4) — a settings UI is a later step."
    />
  );
}
