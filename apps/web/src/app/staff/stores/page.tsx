import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function StaffStoresPage() {
  return (
    <PlaceholderPage
      title="Stores"
      description="Store and fulfilment-node management already has real data (backlog Step 4) — a management UI is a later step."
    />
  );
}
