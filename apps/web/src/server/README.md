# Server-only code

Database clients, Server Action helpers, and other server-only utilities
that don't belong to a single feature go here. Nothing here should be
importable from a Client Component — see `docs/architecture.md` §19 for the
Server Actions / route handlers / database commands split.
